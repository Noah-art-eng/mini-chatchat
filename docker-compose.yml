services:
  backend:
    extends:
      file: docker-compose.dev.yml
      service: backend

  frontend:
    extends:
      file: docker-compose.dev.yml
      service: frontend

networks:
  mini-chatchat:
    driver: bridge
